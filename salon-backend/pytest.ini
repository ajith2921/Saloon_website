[pytest]
# Add the backend root to sys.path so that `from app.xxx import yyy` works
# without needing `python -m pytest` or PYTHONPATH manipulation.
pythonpath = .
testpaths = tests
addopts = -v --tb=short
